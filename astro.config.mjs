import { defineConfig } from 'astro/config';

// Sitio de portfolio — configuración base de Astro.
// Cuando conectes el dominio en Vercel, podés (opcionalmente) agregar
// site: 'https://tu-dominio.com' acá para que se generen mejor los meta tags.
export default defineConfig({
  output: 'static',
});
