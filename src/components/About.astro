---
---

<section class="about section" id="sobre-mi">
  <div class="container about__grid">
    <div>
      <p class="eyebrow">Sobre mí</p>
      <h2 class="about__title">
        <!-- TODO: escribí tu presentación real acá. -->
        Diseñador gráfico y motion designer.
      </h2>
    </div>
    <div class="about__body">
      <p>
        <!-- TODO: reemplazá por tu historia real: formación, años de
        experiencia, tipo de proyectos/clientes, software que usás. -->
        Cuento acá quién soy, cómo llegué al diseño y qué tipo de proyectos
        me interesan. También puedo mencionar el software que uso
        (Illustrator, Photoshop, After Effects, Figma, etc.) y algún dato que
        me distinga.
      </p>
      <ul class="about__tools">
        <li>Illustrator</li>
        <li>Photoshop</li>
        <li>After Effects</li>
        <li>Figma</li>
      </ul>
    </div>
  </div>
</section>

<style>
  .about__grid {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 48px;
  }

  .about__title {
    font-size: 28px;
    max-width: 14ch;
  }

  .about__body p {
    font-size: 17px;
    color: var(--text-muted);
    max-width: 60ch;
  }

  .about__tools {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    list-style: none;
    padding: 0;
    margin-top: 24px;
  }

  .about__tools li {
    font-size: 13px;
    padding: 6px 14px;
    border: 1px solid var(--border);
    border-radius: 999px;
    color: var(--text-muted);
  }

  @media (max-width: 720px) {
    .about__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
